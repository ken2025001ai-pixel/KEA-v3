# Phase 6 Preprocess Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 改造 Phase 6，在 Dispatch 前用 `kea mermaid` + `kea parse` 预处理流程图和对象上下文，subagent 消费结构化文本而非自己读文件。

**Architecture:** Phase 6 新增 Step 2.5 顺序运行两个工具，生成 FLOWCHART_CANDIDATES（流程图节点/边摘要）和 OBJECT_REGISTRY（对象名+属性清单），替换 Dispatch prompt 中的 DIAGRAMS_DIR 和 OBJECTS_DIR 路径。extract-logic SKILL.md 同步更新 Inputs 表和 Step 1，禁止 subagent 自己读文件。

**Tech Stack:** Markdown（AgentFile 指令文件），无 Python 工具层改动

---

## File Map

| 文件 | 改动类型 | 职责 |
|---|---|---|
| `AgentFile/phases/phase-6-extract-logic.md` | Modify | 新增 Step 2.5；Step 3 Dispatch 参数替换；输入参数表注释更新 |
| `AgentFile/skills/extract-logic/SKILL.md` | Modify | Inputs 表更新；Source priority 更新；Step 1 重写；NEEDS_CONTEXT 示例修正 |

无 Python 文件改动，无测试文件（AgentFile 是 Markdown 指令，无法自动测试）。

---

## Task 1：Phase 6 新增 Step 2.5

**Files:**
- Modify: `AgentFile/phases/phase-6-extract-logic.md`

- [ ] **Step 1: 在 Step 2 和 Step 3 之间插入 Step 2.5**

在 `### Step 2: 更新 chain-state.md 为 in_progress` 之后、`### Step 3: Dispatch 逻辑提取 Agent` 之前插入以下内容（完整文本）：

```markdown
### Step 2.5: 预处理流程图与对象上下文

**工具调用 1**：扫描流程图结构

```bash
python3 -m kea --format json mermaid {DIAGRAMS_DIR}
```

**成功**（`count > 0`）→ 生成 `FLOWCHART_CANDIDATES` 文本：

```
流程图扫描结果（{count} 个文件，{总节点数} 个节点，{总边数} 条边）：

文件：{chart.file}
  节点：{label}[{type}], {label}[{type}] ...
  边标签：{edge.label}, {edge.label} ...（仅非空 label）
```

**失败**（`count = 0` 或命令报错）→ 展示错误详情，询问用户：
- A 检查 DIAGRAMS_DIR 路径后重试
- B 跳过工具扫描（fallback：Step 3 仍传 `DIAGRAMS_DIR`，subagent 自行读文件）

---

**工具调用 2**：扫描对象注册表

```bash
python3 -m kea --format json parse {OBJECTS_DIR}
```

**成功**（`count > 0`）→ 从 JSON 中筛选 `type=object` 的文档，生成 `OBJECT_REGISTRY` 文本：

```
已确认对象（{N} 个）：

{对象名} (id={id})
  属性：{属性名}[{类型},主键], {属性名}[{类型}] ...

{对象名} (id={id})
  属性：...
```

**失败**（`count = 0` 或命令报错）→ 展示错误详情，询问用户：
- A 检查 OBJECTS_DIR 路径后重试
- B 跳过对象扫描（fallback：Step 3 仍传 `OBJECTS_DIR`，subagent 自行读文件）
```

- [ ] **Step 2: 修改 Step 3 的 Dispatch 参数块**

将 Step 3 中现有的 Dispatch 块：

```
DIAGRAMS_DIR: {DIAGRAMS_DIR}
SUMMARY_PATHS: {RESEARCH_REPORT_PATH},{INTERVIEW_SUMMARY_PATH}
OBJECTS_DIR: {OBJECTS_DIR}
LOGIC_DIR: {VAULT_PATH}/30-Ontology/logic/{DOMAIN_EN}/
EXISTING_FILES: {EXISTING_FILES（如有）}
```

替换为：

```
FLOWCHART_CANDIDATES: {Step 2.5 生成的流程图扫描文本}
OBJECT_REGISTRY: {Step 2.5 生成的对象注册表文本}
SUMMARY_PATHS: {RESEARCH_REPORT_PATH},{INTERVIEW_SUMMARY_PATH}
LOGIC_DIR: {VAULT_PATH}/30-Ontology/logic/{DOMAIN_EN}/
EXISTING_FILES: {EXISTING_FILES（如有）}
```

- [ ] **Step 3: 更新文件顶部输入参数表**

将参数表中 `DIAGRAMS_DIR` 和 `OBJECTS_DIR` 两行的说明列分别改为：

```
| `DIAGRAMS_DIR` | G3/G4 已校验流程图目录（Step 2.5 由工具扫描，结果以 FLOWCHART_CANDIDATES 形式传入 subagent） |
| `OBJECTS_DIR`  | Phase 5 输出的对象文档目录（Step 2.5 由工具扫描，结果以 OBJECT_REGISTRY 形式传入 subagent） |
```

- [ ] **Step 4: 验证文件结构正确**

```bash
cd /Users/kenkangning/KEA-v3
grep -n "Step 2.5\|Step 3\|FLOWCHART_CANDIDATES\|OBJECT_REGISTRY\|DIAGRAMS_DIR\|OBJECTS_DIR" AgentFile/phases/phase-6-extract-logic.md
```

期望：
- Step 2.5 出现在 Step 3 之前
- Step 3 的 Dispatch 块中含 `FLOWCHART_CANDIDATES` 和 `OBJECT_REGISTRY`
- Step 3 的 Dispatch 块中**不再**含 `DIAGRAMS_DIR: {DIAGRAMS_DIR}` 或 `OBJECTS_DIR: {OBJECTS_DIR}`（参数表中的说明行仍保留这两个名称，但 Dispatch 块中已移除）

- [ ] **Step 5: Commit**

```bash
git add AgentFile/phases/phase-6-extract-logic.md
git commit -m "feat: phase-6 add step 2.5 kea mermaid+parse preprocess before subagent dispatch"
```

---

## Task 2：extract-logic SKILL.md 改造

**Files:**
- Modify: `AgentFile/skills/extract-logic/SKILL.md`

- [ ] **Step 1: 更新 Source Documents 表**

找到 `### Source Documents (what to extract from)` 下的表格：

```markdown
| `DIAGRAMS_DIR` | **Yes** | **Primary source**. Directory containing validated Mermaid flowcharts (Phase 3/4 output). Each flowchart generally maps 1:1 to a logic document. |
| `SUMMARY_PATHS` | **Yes** | Supplementary source. Research reports and interview summaries for business context and detail not captured in flowcharts. |
| `REPORT_PATHS` | No | Additional context files for detail. |
```

替换为：

```markdown
| `FLOWCHART_CANDIDATES` | **Yes** | **Primary source**. Structured flowchart text generated by Phase 6 via `kea mermaid`. Each file entry maps to one logic document. **Do NOT read files from DIAGRAMS_DIR** — the tool has already done that. |
| `OBJECT_REGISTRY` | **Yes** | Confirmed object list with properties, generated by Phase 6 via `kea parse`. Use as sole source for `[[链接]]` validity and attribute-level pseudocode references. **Do NOT read files from OBJECTS_DIR**. |
| `SUMMARY_PATHS` | **Yes** | Supplementary source. Research reports and interview summaries for business context and detail not captured in flowcharts. |
```

- [ ] **Step 2: 更新 Source priority**

找到：
```
**Source priority**: `DIAGRAMS_DIR` > `SUMMARY_PATHS` > `REPORT_PATHS`
```

替换为：
```
**Source priority**: `FLOWCHART_CANDIDATES` > `OBJECT_REGISTRY` > `SUMMARY_PATHS`
```

- [ ] **Step 3: 更新 Upstream Artifacts 表**

找到 `### Upstream Artifacts (cross-reference constraints)` 下的表格：

```markdown
| `OBJECTS_DIR` | **Yes** | Phase 5 output. Must read all object docs to know which objects are available for `[[links]]`. **Never link to non-existent objects.** |
```

替换为：

```markdown
| `OBJECT_REGISTRY` | **Yes** | Phase 6 pre-processed output from `kea parse {OBJECTS_DIR}`. Contains all available object names, ids, and properties. Use this instead of reading OBJECTS_DIR directly. **Never link to objects not listed here.** |
```

- [ ] **Step 4: 重写 Step 1**

将 `## Step 1: Read inputs` 整节（从 `## Step 1: Read inputs` 到下一个 `## Step 2` 之前）替换为：

```markdown
## Step 1: Read inputs

Read `FLOWCHART_CANDIDATES` — structured flowchart text generated by Phase 6
using `kea mermaid`. Each file entry maps to one logic document. Nodes (with
type) and edges provide the pseudocode structure.

Read `OBJECT_REGISTRY` — the complete list of confirmed objects with their
properties, generated by Phase 6 using `kea parse`. Use this as the sole
source for:
- `[[链接]]` validity: only link to objects listed here. If an object is not
  in OBJECT_REGISTRY, do NOT `[[link]]` it — mark it as "待提取对象" instead.
- Attribute-level pseudocode: reference `ObjectName.propertyName`
  (e.g., `采购订单.状态`), not just `ObjectName`.

Then read files in `SUMMARY_PATHS` for supplementary business context —
process purpose, business rules, edge cases not visible in flowcharts.

**Do NOT read files from DIAGRAMS_DIR or OBJECTS_DIR** — the tools have
already done that. Your role is semantic judgment and document generation,
not file parsing.

Map node types to pseudocode constructs (per mermaid-spec.md):

| Node type | Shape | Pseudocode construct |
|---|---|---|
| `action` | `[矩形]` | `call_action("动作名", {params})` |
| `activity` | `(圆角矩形)` | 展开为多个子步骤 |
| `decision` | `{菱形}` | `if condition: / else:` — 必须有 Yes/No 两个分支 |
| `subprocess` | `[[双框]]` | `result = call_logic("子流程名", {params})` |
| `recursive` | `{{双花括号}}` | `for item in collection:` |
| START | `((开始))` | `function name(inputs):` |
| END | `((结束))` / `>结束]` | `return {...}` |
```

- [ ] **Step 5: 修正 NEEDS_CONTEXT 示例**

找到 NEEDS_CONTEXT 返回状态示例中：
```
- {具体说明，如"DIAGRAMS_DIR 为空，无流程图文件"}
```

替换为：
```
- {具体说明，如"FLOWCHART_CANDIDATES 为空，无流程图数据"}
```

- [ ] **Step 6: 验证无残留错误引用**

```bash
grep -n "DIAGRAMS_DIR\|OBJECTS_DIR\|REPORT_PATHS" AgentFile/skills/extract-logic/SKILL.md
```

期望：
- `DIAGRAMS_DIR` 只出现在 "Do NOT read files from DIAGRAMS_DIR" 这类禁止性说明中
- `OBJECTS_DIR` 只出现在 "Do NOT read files from OBJECTS_DIR" 这类禁止性说明中
- `REPORT_PATHS` 不出现

- [ ] **Step 7: Commit**

```bash
git add AgentFile/skills/extract-logic/SKILL.md
git commit -m "feat: extract-logic skill consumes FLOWCHART_CANDIDATES+OBJECT_REGISTRY instead of reading dirs"
```

---

## Task 3：全量验证

- [ ] **Step 1: 运行全部测试（确保无回归）**

```bash
cd /Users/kenkangning/KEA-v3
python3 -m pytest tests/ -q 2>&1 | tail -5
```

期望：`52 passed`（或更多，无 failed）

- [ ] **Step 2: kea lint 验证 AgentFile**

```bash
python3 -m kea --format json lint AgentFile/ 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); print('errors:', d.get('summary',{}).get('errors',0))"
```

期望：`errors: 0`

- [ ] **Step 3: 人工核查 Phase 6 关键结构**

```bash
grep -n "Step 2\|Step 3\|FLOWCHART\|OBJECT_REGISTRY\|DIAGRAMS_DIR\|OBJECTS_DIR" AgentFile/phases/phase-6-extract-logic.md | head -20
```

确认：
- Step 2.5 在 Step 3 之前
- Step 3 Dispatch 块含 `FLOWCHART_CANDIDATES` 和 `OBJECT_REGISTRY`
- Step 3 Dispatch 块不含裸路径参数 `DIAGRAMS_DIR: {DIAGRAMS_DIR}`

- [ ] **Step 4: 人工核查 extract-logic SKILL.md 关键结构**

```bash
grep -n "FLOWCHART_CANDIDATES\|OBJECT_REGISTRY\|DIAGRAMS_DIR\|OBJECTS_DIR\|REPORT_PATHS\|Source priority" AgentFile/skills/extract-logic/SKILL.md
```

确认：
- `FLOWCHART_CANDIDATES` 和 `OBJECT_REGISTRY` 出现在 Inputs 表和 Step 1
- `DIAGRAMS_DIR` / `OBJECTS_DIR` 只出现在 "Do NOT read" 禁止性说明中
- `REPORT_PATHS` 不出现
- Source priority 为 `FLOWCHART_CANDIDATES > OBJECT_REGISTRY > SUMMARY_PATHS`
