# Phase 7+8 Preprocess Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 改造 Phase 7 和 Phase 8，在 Dispatch 前用 `kea parse` 预处理上游文档生成 Registry，subagent 消费结构化注册表而非自己读目录。

**Architecture:** Phase 7 新增 Step 2.5（LOGIC_REGISTRY + OBJECT_REGISTRY）；Phase 8 新增 Step 2.5（LOGIC_REGISTRY + ACTION_REGISTRY + OBJECT_REGISTRY）；对应 SKILL.md 同步更新 Inputs 表和 Step 1，禁止 subagent 自己读目录。kea 工具层无需改动。

**Tech Stack:** Markdown（AgentFile 指令文件），无 Python 工具层改动

---

## File Map

| 文件 | 改动类型 | 职责 |
|---|---|---|
| `AgentFile/phases/phase-7-extract-actions.md` | Modify | 新增 Step 2.5；Step 4 Dispatch 参数替换；输入参数表注释更新 |
| `AgentFile/skills/extract-actions/SKILL.md` | Modify | Inputs 表、Source priority、Upstream Artifacts、Step 1 重写、NEEDS_CONTEXT 修正 |
| `AgentFile/phases/phase-8-extract-rules.md` | Modify | 新增 Step 2.5；Step 4 Dispatch 参数替换；输入参数表注释更新 |
| `AgentFile/skills/extract-rules/SKILL.md` | Modify | Inputs 表、Source priority、Upstream Artifacts、Step 1 重写、NEEDS_CONTEXT 修正 |

无 Python 文件改动，无测试文件。

---

## Task 1：Phase 7 phase 文件改造

**Files:**
- Modify: `AgentFile/phases/phase-7-extract-actions.md`

- [ ] **Step 1: 在 Step 2 和 Step 4 之间插入 Step 2.5**

在 `### Step 2: 更新 chain-state.md 为 in_progress` 之后、`### Step 4: Dispatch 动作提取 Agent` 之前，插入以下完整内容：

````markdown
### Step 2.5: 预处理逻辑注册表与对象注册表

**工具调用 1**：扫描逻辑文档

```bash
python3 -m kea --format json parse {LOGIC_DIR}
```

**成功**（`count > 0`）→ 筛选 `type=logic`，生成 `LOGIC_REGISTRY` 文本：

```
已确认逻辑（{N} 个）：

{逻辑名} (id={id})
  关联动作：{动作名1}, {动作名2} ...（来自 relations type=calls）
  输入对象：{对象名1}, {对象名2} ...
  输出对象：{对象名1} ...

{逻辑名} (id={id})
  ...
```

**失败**（count = 0 或命令报错）→ 展示错误详情，询问用户：
- A 检查 LOGIC_DIR 路径后重试
- B 跳过工具扫描（fallback：Step 4 仍传 `LOGIC_DIR`，subagent 自行读文件）

---

**工具调用 2**：扫描对象文档

```bash
python3 -m kea --format json parse {OBJECTS_DIR}
```

**成功**（`count > 0`）→ 生成 `OBJECT_REGISTRY` 文本：

```
已确认对象（{N} 个）：

{对象名} (id={id})
  属性：{属性名}[{类型},主键], {属性名}[{类型}] ...
```

**失败** → 展示错误详情，询问用户：
- A 检查 OBJECTS_DIR 路径后重试
- B 跳过扫描（fallback：Step 4 仍传 `OBJECTS_DIR`）
````

- [ ] **Step 2: 替换 Step 4 的 Dispatch 参数块**

将 Step 4 中现有的 Dispatch 块：

```
LOGIC_DIR: {LOGIC_DIR}
OBJECTS_DIR: {OBJECTS_DIR}
ACTIONS_DIR: {VAULT_PATH}/30-Ontology/actions/{DOMAIN_EN}/
SUMMARY_PATHS: {RESEARCH_REPORT_PATH},{INTERVIEW_SUMMARY_PATH}
EXISTING_FILES: {EXISTING_FILES（如有）}
```

替换为：

```
REFERENCED_ACTIONS: {Step 3 已收集的动作候选列表文本}
LOGIC_REGISTRY: {Step 2.5 生成的逻辑注册表文本}
OBJECT_REGISTRY: {Step 2.5 生成的对象注册表文本}
ACTIONS_DIR: {VAULT_PATH}/30-Ontology/actions/{DOMAIN_EN}/
SUMMARY_PATHS: {RESEARCH_REPORT_PATH},{INTERVIEW_SUMMARY_PATH}
EXISTING_FILES: {EXISTING_FILES（如有）}
```

- [ ] **Step 3: 更新文件顶部输入参数表**

将参数表中 `LOGIC_DIR` 和 `OBJECTS_DIR` 行的说明列改为：

```
| `LOGIC_DIR`   | Phase 6 输出逻辑目录（Step 2.5 由工具扫描，结果以 LOGIC_REGISTRY 形式传入 subagent） |
| `OBJECTS_DIR` | Phase 5 输出对象目录（Step 2.5 由工具扫描，结果以 OBJECT_REGISTRY 形式传入 subagent） |
```

- [ ] **Step 4: 验证结构正确**

```bash
grep -n "Step 2.5\|LOGIC_REGISTRY\|OBJECT_REGISTRY\|REFERENCED_ACTIONS" AgentFile/phases/phase-7-extract-actions.md
```

期望：Step 2.5 行号 < Step 4 行号，三个新参数名均出现在 Dispatch 块。

- [ ] **Step 5: Commit**

```bash
git add AgentFile/phases/phase-7-extract-actions.md
git commit -m "feat: phase-7 add step 2.5 kea parse preprocess before subagent dispatch"
```

---

## Task 2：extract-actions SKILL.md 改造

**Files:**
- Modify: `AgentFile/skills/extract-actions/SKILL.md`

- [ ] **Step 1: 替换 Source Documents 表**

找到 `### Source Documents (what to extract from)` 下的表格（含 SUMMARY_PATHS 和 REPORT_PATHS 两行），整表替换为：

```markdown
| `REFERENCED_ACTIONS` | **Yes** | **Primary source**. Pre-computed list of action names and reference counts, collected by Phase 7 from logic documents. This is your complete extraction scope. **Do NOT read files from LOGIC_DIR.** |
| `LOGIC_REGISTRY` | **Yes** | Structured logic document index generated by Phase 7 via `kea parse`. Use to fill `part_of` relations: find which logic id(s) call each action via the "关联动作" field. |
| `OBJECT_REGISTRY` | **Yes** | Structured object index generated by Phase 7 via `kea parse`. Use for `uses`/`modifies` relation declarations and `[[链接]]` validity. **Do NOT read files from OBJECTS_DIR.** |
| `SUMMARY_PATHS` | No | Supplementary business context (parameter descriptions, exception scenarios). |
```

- [ ] **Step 2: 更新 Source priority**

找到：
```
**Source priority**: `DIAGRAMS_DIR` > `SUMMARY_PATHS` > `REPORT_PATHS`
```
（或当前文件中实际存在的 Source priority 行），替换为：
```
**Source priority**: `REFERENCED_ACTIONS` > `LOGIC_REGISTRY` > `OBJECT_REGISTRY` > `SUMMARY_PATHS`
```

- [ ] **Step 3: 替换 Upstream Artifacts 表**

找到 `### Upstream Artifacts (cross-reference constraints)` 下的表格（含 LOGIC_DIR 和 OBJECTS_DIR 两行），整表替换为：

```markdown
| `LOGIC_REGISTRY` | **Yes** | Phase 7 pre-processed output from `kea parse {LOGIC_DIR}`. Use to identify which logic id calls each action for `part_of` declarations. Never reference logics not listed here. |
| `OBJECT_REGISTRY` | **Yes** | Phase 7 pre-processed output from `kea parse {OBJECTS_DIR}`. Use for object link validation and `uses`/`modifies` relation declarations. Never link to objects not listed here. |
```

- [ ] **Step 4: 重写 Step 1**

将 `## Step 1: Read inputs` 整节（从标题到下一个 `## Step 2` 之前）替换为：

```markdown
## Step 1: Read inputs

Read `REFERENCED_ACTIONS` — the pre-computed list of action names and their
reference counts, collected by Phase 7 from logic documents. This is your
complete extraction scope. **Do NOT read files from LOGIC_DIR.**

Read `LOGIC_REGISTRY` — structured logic document index generated by Phase 7
using `kea parse`. Use it to fill `part_of` relations: for each action in
REFERENCED_ACTIONS, find which logic id(s) call it via the "关联动作" field.

Read `OBJECT_REGISTRY` — structured object index generated by Phase 7 using
`kea parse`. Use it for:
- `uses`/`modifies` relation declarations (which objects does this action read/write)
- `[[链接]]` validity: only link to objects listed here. **Do NOT read files from OBJECTS_DIR.**

Read files in `SUMMARY_PATHS` (if provided) for supplementary detail on action
parameters, trigger conditions, and exception scenarios.

**Do NOT read files from LOGIC_DIR or OBJECTS_DIR** — the tools have already done that.
```

- [ ] **Step 5: 修正 NEEDS_CONTEXT 示例**

找到 NEEDS_CONTEXT 示例中任何包含 `LOGIC_DIR` 或 `OBJECTS_DIR` 的主动读取描述，替换为：
```
- {具体说明，如"REFERENCED_ACTIONS 为空，无动作候选数据"}
```

- [ ] **Step 6: 验证无残留错误引用**

```bash
grep -n "LOGIC_DIR\|OBJECTS_DIR\|REPORT_PATHS" AgentFile/skills/extract-actions/SKILL.md
```

期望：`LOGIC_DIR` / `OBJECTS_DIR` 只出现在 "Do NOT read" 禁止性说明中，`REPORT_PATHS` 不出现。

- [ ] **Step 7: Commit**

```bash
git add AgentFile/skills/extract-actions/SKILL.md
git commit -m "feat: extract-actions skill consumes REFERENCED_ACTIONS+LOGIC_REGISTRY+OBJECT_REGISTRY"
```

---

## Task 3：Phase 8 phase 文件改造

**Files:**
- Modify: `AgentFile/phases/phase-8-extract-rules.md`

- [ ] **Step 1: 在 Step 3 和 Step 4 之间插入 Step 2.5**

在 `### Step 3: 更新 chain-state.md 为 in_progress` 之后、`### Step 4: Dispatch 规则提取 Agent` 之前，插入以下完整内容：

````markdown
### Step 2.5: 预处理逻辑/动作/对象注册表

**工具调用 1**：扫描逻辑文档

```bash
python3 -m kea --format json parse {LOGIC_DIR}
```

**成功**（`count > 0`）→ 筛选 `type=logic`，生成 `LOGIC_REGISTRY` 文本：

```
已确认逻辑（{N} 个）：

{逻辑名} (id={id})
  前置条件：{前置条件描述} ...
  关联动作：{动作名1}, {动作名2} ...
```

**失败** → 展示错误详情，询问用户：
- A 检查 LOGIC_DIR 路径后重试
- B 跳过扫描（fallback：Step 4 仍传 `LOGIC_DIR`）

---

**工具调用 2**：扫描动作文档

```bash
python3 -m kea --format json parse {ACTIONS_DIR}
```

**成功**（`count > 0`）→ 筛选 `type=action`，生成 `ACTION_REGISTRY` 文本：

```
已确认动作（{N} 个）：

{动作名} (id={id})
  所属逻辑：{逻辑名} (id={id})
  输入约束：{参数名}[{类型},required={true|false}] ...
  操作对象：{对象名}[uses|modifies] ...
```

**失败** → 展示错误详情，询问用户：
- A 检查 ACTIONS_DIR 路径后重试
- B 跳过扫描（fallback：Step 4 仍传 `ACTIONS_DIR`）

---

**工具调用 3**：扫描对象文档

```bash
python3 -m kea --format json parse {OBJECTS_DIR}
```

**成功**（`count > 0`）→ 生成 `OBJECT_REGISTRY` 文本：

```
已确认对象（{N} 个）：

{对象名} (id={id})
  属性：{属性名}[{类型},主键], {属性名}[{类型}] ...
```

**失败** → 询问 A 重试 / B 跳过（fallback：Step 4 仍传 `OBJECTS_DIR`）
````

- [ ] **Step 2: 替换 Step 4 的 Dispatch 参数块**

将 Step 4 中现有的 Dispatch 块：

```
LOGIC_DIR: {LOGIC_DIR}
ACTIONS_DIR: {ACTIONS_DIR}
OBJECTS_DIR: {OBJECTS_DIR}
RULES_DIR: {VAULT_PATH}/30-Ontology/rules/{DOMAIN_EN}/
SUMMARY_PATHS: {RESEARCH_REPORT_PATH}
补充提取目标: {用户确认的候选规则名（如有范围限制）}
```

替换为：

```
RULE_CANDIDATES: {Step 1/2 收集并经用户确认的规则候选列表文本}
LOGIC_REGISTRY: {Step 2.5 生成的逻辑注册表文本}
ACTION_REGISTRY: {Step 2.5 生成的动作注册表文本}
OBJECT_REGISTRY: {Step 2.5 生成的对象注册表文本}
RULES_DIR: {VAULT_PATH}/30-Ontology/rules/{DOMAIN_EN}/
SUMMARY_PATHS: {RESEARCH_REPORT_PATH}
补充提取目标: {用户确认的候选规则名（如有范围限制）}
EXISTING_FILES: {EXISTING_FILES（如有）}
```

- [ ] **Step 3: 更新文件顶部输入参数表**

将参数表中 `LOGIC_DIR`、`ACTIONS_DIR`、`OBJECTS_DIR` 三行的说明列改为：

```
| `LOGIC_DIR`   | logic 目录（Step 2.5 由工具扫描，结果以 LOGIC_REGISTRY 形式传入 subagent） |
| `ACTIONS_DIR` | actions 目录（Step 2.5 由工具扫描，结果以 ACTION_REGISTRY 形式传入 subagent） |
| `OBJECTS_DIR` | objects 目录（Step 2.5 由工具扫描，结果以 OBJECT_REGISTRY 形式传入 subagent） |
```

- [ ] **Step 4: 验证结构正确**

```bash
grep -n "Step 2.5\|LOGIC_REGISTRY\|ACTION_REGISTRY\|OBJECT_REGISTRY\|RULE_CANDIDATES" AgentFile/phases/phase-8-extract-rules.md
```

期望：Step 2.5 行号 < Step 4 行号，四个新参数名均出现。

- [ ] **Step 5: Commit**

```bash
git add AgentFile/phases/phase-8-extract-rules.md
git commit -m "feat: phase-8 add step 2.5 kea parse preprocess before subagent dispatch"
```

---

## Task 4：extract-rules SKILL.md 改造

**Files:**
- Modify: `AgentFile/skills/extract-rules/SKILL.md`

- [ ] **Step 1: 替换 Source Documents 表**

找到 `### Source Documents (what to extract from)` 下的表格（含 SUMMARY_PATHS 和 REPORT_PATHS 两行），整表替换为：

```markdown
| `RULE_CANDIDATES` | **Yes** | **Primary source**. Pre-identified and user-confirmed list of rule candidates, collected by Phase 8. Each entry includes rule name, type classification, source document, and condition text. This is your complete extraction scope. **Do NOT re-scan LOGIC_DIR, ACTIONS_DIR, or OBJECTS_DIR.** |
| `LOGIC_REGISTRY` | **Yes** | Structured logic index from `kea parse`. Use for `guards` relation declarations (which logic id does this rule guard). |
| `ACTION_REGISTRY` | **Yes** | Structured action index from `kea parse`. Use for `constrains` relation declarations on actions. |
| `OBJECT_REGISTRY` | **Yes** | Structured object index from `kea parse`. Use for `constrains` relation declarations on objects and field-level reference validation in `rule_expression`. |
| `SUMMARY_PATHS` | No | Supplementary policy context. |
```

- [ ] **Step 2: 更新 Source priority**

找到当前文件中的 Source priority 行，替换为：
```
**Source priority**: `RULE_CANDIDATES` > `LOGIC_REGISTRY` > `ACTION_REGISTRY` > `OBJECT_REGISTRY` > `SUMMARY_PATHS`
```

- [ ] **Step 3: 替换 Upstream Artifacts 表**

找到 `### Upstream Artifacts (cross-reference constraints)` 下的表格（含 LOGIC_DIR、ACTIONS_DIR、OBJECTS_DIR 三行），整表替换为：

```markdown
| `LOGIC_REGISTRY` | **Yes** | Phase 8 pre-processed output from `kea parse {LOGIC_DIR}`. Use for `guards` relation declarations. |
| `ACTION_REGISTRY` | **Yes** | Phase 8 pre-processed output from `kea parse {ACTIONS_DIR}`. Use for `constrains` relation declarations on actions. |
| `OBJECT_REGISTRY` | **Yes** | Phase 8 pre-processed output from `kea parse {OBJECTS_DIR}`. Use for `constrains` relation declarations on objects and field validation. Never reference fields not listed here. |
```

- [ ] **Step 4: 重写 Step 1**

将 `## Step 1: Read inputs and identify rule candidates` 整节替换为：

```markdown
## Step 1: Read inputs and identify rule candidates

Read `RULE_CANDIDATES` — pre-identified and user-confirmed list of rule candidates,
collected by Phase 8 from all upstream documents. Each entry includes rule name,
type classification, source document, and condition text. This is your complete
extraction scope. **Do NOT re-scan LOGIC_DIR, ACTIONS_DIR, or OBJECTS_DIR.**

Read `LOGIC_REGISTRY` for `guards` relation declarations (which logic id does
this rule guard as a precondition).

Read `ACTION_REGISTRY` for `constrains` relation declarations on actions (which
action id does this validation/guard rule constrain).

Read `OBJECT_REGISTRY` for `constrains` relation declarations on objects and for
field-level reference validation (e.g., `采购订单.总金额` must exist in OBJECT_REGISTRY
before it can appear in `rule_expression`).

Read `SUMMARY_PATHS` (if provided) for supplementary policy context.

**Do NOT read files from LOGIC_DIR, ACTIONS_DIR, or OBJECTS_DIR** — the tools
have already done that. The rule candidates and their context are fully captured in
RULE_CANDIDATES.
```

- [ ] **Step 5: 修正 NEEDS_CONTEXT 示例**

找到 NEEDS_CONTEXT 示例中任何含目录路径的主动读取描述，替换为：
```
- {具体说明，如"RULE_CANDIDATES 为空，无规则候选数据"}
```

- [ ] **Step 6: 验证无残留错误引用**

```bash
grep -n "LOGIC_DIR\|ACTIONS_DIR\|OBJECTS_DIR\|REPORT_PATHS" AgentFile/skills/extract-rules/SKILL.md
```

期望：三个目录参数只出现在 "Do NOT read" 禁止性说明中，`REPORT_PATHS` 不出现。

- [ ] **Step 7: Commit**

```bash
git add AgentFile/skills/extract-rules/SKILL.md
git commit -m "feat: extract-rules skill consumes RULE_CANDIDATES+LOGIC_REGISTRY+ACTION_REGISTRY+OBJECT_REGISTRY"
```

---

## Task 5：全量验证

- [ ] **Step 1: 全量测试**

```bash
cd /Users/kenkangning/KEA-v3
python3 -m pytest tests/ -q 2>&1 | tail -5
```

期望：全部 PASSED，无失败。

- [ ] **Step 2: kea lint AgentFile**

```bash
python3 -m kea --format json lint AgentFile/ 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); print('errors:', d.get('summary',{}).get('errors',0))"
```

期望：`errors: 0`

- [ ] **Step 3: 核查 Phase 7 结构**

```bash
grep -n "Step 2.5\|LOGIC_REGISTRY\|OBJECT_REGISTRY\|REFERENCED_ACTIONS\|LOGIC_DIR.*{LOGIC\|OBJECTS_DIR.*{OBJ" AgentFile/phases/phase-7-extract-actions.md | head -15
```

确认：Step 2.5 在 Step 4 之前；Dispatch 块含新参数，不含裸路径 `LOGIC_DIR: {LOGIC_DIR}` 或 `OBJECTS_DIR: {OBJECTS_DIR}`。

- [ ] **Step 4: 核查 Phase 8 结构**

```bash
grep -n "Step 2.5\|LOGIC_REGISTRY\|ACTION_REGISTRY\|OBJECT_REGISTRY\|RULE_CANDIDATES" AgentFile/phases/phase-8-extract-rules.md | head -15
```

确认：Step 2.5 在 Step 4 之前；五个新参数名均出现。

- [ ] **Step 5: 核查 extract-actions SKILL.md**

```bash
grep -n "LOGIC_DIR\|OBJECTS_DIR\|REPORT_PATHS\|Source priority" AgentFile/skills/extract-actions/SKILL.md
```

确认：`LOGIC_DIR` / `OBJECTS_DIR` 只出现在 "Do NOT read" 中，`REPORT_PATHS` 不出现。

- [ ] **Step 6: 核查 extract-rules SKILL.md**

```bash
grep -n "LOGIC_DIR\|ACTIONS_DIR\|OBJECTS_DIR\|REPORT_PATHS\|Source priority" AgentFile/skills/extract-rules/SKILL.md
```

确认：三个目录参数只出现在 "Do NOT read" 中，`REPORT_PATHS` 不出现。
